#include <stdio.h>

int main(void) {
  int a,b,s;
  printf("Enter value of a =");
  scanf("%d",&a);
  printf("Enter value of b =");
  scanf("%d",&b);
  s=a+b;
  printf("\nSum=");
  printf("%d",s);
  return 0;
}
